## Build notes

- YYYY-MM-DD: Started implementation.

## Spec Updates

List every `docs/specs/*` file changed in this story (path + one-line description per spec).
If no spec was impacted, write `_No spec impact_` and reference the rationale recorded under
`## Documentation and specs to update`.

- `docs/specs/<file>.md` — <what changed>
