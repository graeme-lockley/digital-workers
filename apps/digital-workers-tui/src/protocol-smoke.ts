import { CANONICAL_PROTOCOL_VERSION } from "@digital-workers/protocol";

// Compile-time and runtime smoke import proving cross-workspace resolution.
export const protocolVersionSmoke = CANONICAL_PROTOCOL_VERSION;
